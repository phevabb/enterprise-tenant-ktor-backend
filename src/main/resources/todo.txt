com.example.staff
├── StaffModule.kt
├── StaffRoutes.kt
├── StaffService.kt
├── StaffModels.kt


com.example.students
├── StudentModule.kt
├── StudentRoutes.kt
├── StudentService.kt
├── StudentModels.kt


com.example.staff
├── StaffModule.kt      ✅ package entry point
├── StaffRoutes.kt      ✅ controllers / routs
├── StaffService.kt     ✅ business / view logic
├── StaffRepository.kt  ✅ database access
├── StaffModels.kt      ✅ domain models
├── StaffDto.kt         ✅ request/response DTOs


account/
  AccountModule.kt       (sends routs to main app)
  AccountTable.kt        (DB)
  AccountModels.kt       (Domain)
  AccountDto.kt          (Request/Response DTOs)... serializers
  AccountRepository.kt   (DB access)
  AccountService.kt      (Logic)
  AccountRoutes.kt       (HTTP)

student/
  StudentsTable.kt         # DB schema
  StudentModels.kt         # domain objects
  StudentMappers.kt        # ResultRow -> domain model
  StudentRepository.kt     # database queries
  StudentService.kt        # business logic
  StudentRoutes.kt         # HTTP routes
  StudentModule.kt         # route mount (/students)



error: Failed to convert request body to class com.example.student.dtos.requests.CreateTermRequest
fix:
1. first do this:
    val rawbody =call.receiveText()
    println("rawbody is $rawbody")

   3. checke the rawbody and create something like this for it


@Serializable
data class CreateTermIncoming(
    val name: String,
    val academic_year_id: String
)


then use it

 val incoming = call.receive<CreateTermIncoming>()

 val req = CreateTermRequest(
             name = incoming.name,
             academic_year = incoming.academic_year_id.toInt()
         )


 to create datetime: it[dateCreated] = System.currentTimeMillis()

 to run: mvn clean package

 export students: java -cp .\target\ktor-sample-marvin-0.0.1-jar-with-dependencies.jar com.example.ApplicationKt import-students


 fix search and pagination

 fix unable to deassign familiy to a student

fixed search, and filters for payments, records and feeStructure

 to deploy.

 1. First Check DB in config
 2. uncomment sms in (paymentroute under fees and familypaymentroute under familypaymentroute)
 2. RUN mvn clean package
 3. Push to github







